[inputs]
figma_url = "https://example.com/figma/react-sample"
component_repo_path = "."

[agent]
api = "anthropic"
max_tokens = 4000

[codegen]
max_retries = 4       # Retry attempts on validation failure
concurrency = 8       # Max parallel LLM requests during code generation
